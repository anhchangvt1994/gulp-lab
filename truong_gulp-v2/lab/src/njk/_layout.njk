{% set objResource = objGlobal['resource'] %}

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <title>{{ objResource.projectTitle + '-' + pageHeadTitle }}</title>

    <!-- global css file (common.css) -->
    {% for cssFile in objResource['common']['arrCssFile'] %}
      <link rel="stylesheet" href="{{ LAYOUT_CONFIG.cssUrl + cssFile + '.css' + '?v=' + CACHE_VERSION }}">
    {% endfor %}

    <!-- page css file -->
    {% for cssFile in objResource %}
      <link rel="stylesheet" href="{{ LAYOUT_CONFIG.cssUrl + cssFile + '.css' + '?v=' + CACHE_VERSION }}">
    {% endfor %}

    <!-- global js file (common.js) -->
    {% for jsFile in objResource['common']['arrJsFile'] %}
      <script src="{{ LAYOUT_CONFIG.jsUrl + jsFile + '.js' + '?v=' + CACHE_VERSION }}"></script>
    {% endfor %}
  </head>
  <body class='{{ bodyClass }}'>
    <!-- header region -->
    {% block header %}
      {% include "./global/header-section.njk" %}
    {% endblock %}
    <!-- #end header region -->

    <!-- body region -->
    {% block body %}{% endblock %}
    <!-- #end body region -->

    <!-- footer region -->
    {% block footer %}
      {% include "./global/footer-section.njk" %}
    {% endblock %}
    <!-- #end footer region -->

    <!-- page js file -->
    {% for jsFile in objResource[namepage]['arrJsFile'] %}
      <script src="{{ LAYOUT_CONFIG.jsUrl + jsFile + '.js' + '?v=' + CACHE_VERSION }}"></script>
    {% endfor %}

    <!-- other js file -->
    {% block script %}{% endblock %}
  </body>
</html>
